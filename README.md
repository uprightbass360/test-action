# Test Action

A simple composite action for testing fork-sync workflows.

## Inputs

| Input | Description | Default |
|-------|-------------|---------|
| `greeting` | Greeting to display | `Hello from test-action!` |
| `verbose` | Enable verbose output | `false` |

